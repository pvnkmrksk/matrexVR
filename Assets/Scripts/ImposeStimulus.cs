using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImposeStimulus : MonoBehaviour
{
    // Start is called before the first frame update

    // impose is applying a translation or rotation on the transform.
    // this code will apply impose at a range of speeds as given from the serialized fields
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
